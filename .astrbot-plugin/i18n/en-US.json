{
  "metadata": {
    "display_name": "LLM Tattle Tool",
    "short_desc": "Let the LLM privately message configured UIDs when it wants to complain.",
    "desc": "Registers an LLM tool that sends complaint messages to the UID list configured in WebUI, optionally attaching the most recent chat messages as evidence — by default as a QQ merged-forward chat-log card that forwards the original messages, so images and @ mentions stay intact (OneBot platforms only). Includes auto-ban: users who trigger too many complaints within a time window are blocked from interacting with the bot."
  },
  "config": {
    "receiver_uids": {
      "description": "Complaint receiver UID list",
      "hint": "UIDs that should receive private complaint messages. Messages are sent on the same platform as FriendMessage sessions."
    },
    "message_template": {
      "description": "Complaint message template",
      "hint": "Available variables: {accused}, {reason}, {content}, {sender_id}, {sender_name}, {session_id}, {evidence} (recent chat log; appended at the end when the variable is absent)"
    },
    "forward_recent_messages": {
      "description": "Attach recent messages as evidence",
      "hint": "⚠️ OneBot (aiocqhttp) only — other platforms cannot fetch chat history and are skipped silently. When on, the most recent messages at complaint time are forwarded to the receivers."
    },
    "recent_message_count": {
      "description": "Number of messages to attach",
      "hint": "⚠️ OneBot (aiocqhttp) only. Defaults to 5, capped at 100; larger values are clamped and 0 disables it. Messages longer than 300 characters are truncated."
    },
    "evidence_style": {
      "description": "How the records are presented",
      "hint": "⚠️ OneBot (aiocqhttp) only. `forward` sends a QQ merged-forward chat-log card as a separate message after the complaint; it forwards the original messages (the same mechanism the phone client uses), so images, @ mentions, stickers and voice notes survive intact. `text` inlines the records into the complaint body with timestamps (images become an [image] placeholder) and honours the {evidence} variable. Card sends that fail fall back to text automatically."
    },
    "ban_window_hours": {
      "description": "Violation window x (hours)",
      "hint": "Sliding window used to count violations; decimals allowed. Auto-ban is disabled unless x, y and z are all positive."
    },
    "ban_max_violations": {
      "description": "Max violations y within the window",
      "hint": "A sender who triggers this many complaints inside the window is banned automatically. Set 0 to disable auto-ban."
    },
    "ban_duration_hours": {
      "description": "Ban duration z (hours)",
      "hint": "How long a banned user is blocked from interacting with the bot; decimals allowed."
    },
    "ban_message_template": {
      "description": "Ban notice template",
      "hint": "Message shown when a banned user tries to interact with the bot. Available variable: {banned_time} (remaining ban hours)."
    }
  }
}
