name: astrbot_plugin_tattle
desc: Register an LLM tool that sends complaint messages to configured private UIDs.
help: Configure receiver UIDs in WebUI. The LLM can call complain_to_receivers to send a private complaint.
version: v1.0.0
author: AlanBacker
repo: ""
display_name: LLM 告状机
short_desc: 让 LLM 在想告状时私发消息给指定 UID。
astrbot_version: ">=4.5.7"
